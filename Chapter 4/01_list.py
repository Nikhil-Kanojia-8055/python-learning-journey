# list is an container to store an value to story any datatype
# difference is that strings are immutable they cannot be changed at runtime
# and can happpen in the string lists are mutable

list = ["Nikhil",889,False,"Ram"]
print(list[0]) 

# list changed
list[0] = "Nikhil Kanojia"


# difference between string and list 

print(list[0])

# slicing in lists

print(list[0:3])
s1 = {10,90,59,33}
s2 = {10,41,43,66}

print(s1.union(s2))
# means the value which is in s1 and s2 but did not repeat it

print(s1.intersection(s2))
# only give the values which in both s1 and s2
